import Box from "@mui/material/Box/Box";
import styled from "@mui/material/styles/styled";

const StyledDataTable = styled(Box)(() => {
  return {
    height: "100vh",
    width: "100%",
  };
});

export default StyledDataTable;
