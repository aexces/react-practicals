import { Box, styled } from "@mui/material";

const StyledHeader = styled(Box)(() => {
  return {
    display: "flex",
    padding: 2,
    justifyContent: "end",
  };
});

export default StyledHeader;
